import React from 'react';
import styled from 'styled-components';

const CardContainer = styled.div`
  /* Styles for project card container */
`;

const ProjectCard = ({ project }) => {
  return (
    <CardContainer>
      {/* Project details */}
    </CardContainer>
  );
}

export default ProjectCard;
